"use client"; import { useState } from "react";
const fields: Record<string, [string,string][]> = { DISCORD: [["webhookUrl","Webhook URL"]], GOTIFY: [["serverUrl","Server URL"],["token","Application token"],["priority","Priority"]], PUSHOVER: [["userKey","User key"],["token","Application token"],["priority","Priority"]], TELEGRAM: [["botToken","Bot token"],["chatId","Chat ID"]], SMTP: [["host","SMTP hostname"],["port","SMTP port"],["username","Username"],["password","Password"],["from","From address"],["to","Recipient address"]], WEBHOOK: [["url","URL"],["method","HTTP method"],["bodyTemplate","JSON body template"]] };
export function NotificationSettings({ initial }: { initial: { id: string; name: string; type: string; enabled: boolean }[] }) { const [channels, setChannels] = useState(initial); const [type, setType] = useState("DISCORD"); const [message, setMessage] = useState(""); const submit = async (e: React.FormEvent<HTMLFormElement>) => { e.preventDefault(); const f = new FormData(e.currentTarget); const configuration = Object.fromEntries(fields[type].map(([key]) => [key, f.get(key) ?? ""])); const response = await fetch("/api/notification-channels", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: f.get("name"), type, enabled: true, configuration }) }); const body = await response.json(); if (response.ok) { setChannels((c) => [...c, body.channel]); e.currentTarget.reset(); setMessage("Channel saved. Secrets are encrypted."); } else setMessage(body.error?.message); }; return <div className="settingsSplit"><section className="formCard"><h2>Notification channels</h2><p className="help">Bots can send one combined alert to every selected channel.</p>{channels.length ? <div className="channelList">{channels.map((c) => <div className="channel" key={c.id}><span className="channelIcon">↗</span><div><b>{c.name}</b><small>{c.type}</small></div><span className="badge healthy">Enabled</span><button className="button tiny" onClick={async () => { const r = await fetch(`/api/notification-channels/${c.id}/test`, { method: "POST" }); const b = await r.json(); setMessage(r.ok ? "Test sent successfully." : b.error?.message); }}>Test</button></div>)}</div> : <div className="miniEmpty">No channels configured yet.</div>}</section><form className="formCard" onSubmit={submit}><h2>Add a channel</h2><label>Provider<select value={type} onChange={(e) => setType(e.target.value)}>{Object.keys(fields).map((value) => <option value={value} key={value}>{value}</option>)}</select></label><label>Channel name<input name="name" required placeholder={`My ${type.toLowerCase()}`} /></label>{fields[type].map(([key,label]) => <label key={key}>{label}<input required={!["priority","bodyTemplate","username","password"].includes(key)} name={key} type={key.toLowerCase().includes("token") || key === "password" ? "password" : "text"} /></label>)}<button className="button primary">Save channel</button>{message && <p className="saved">{message}</p>}</form></div>; }
