import { NextResponse } from "next/server"; import { prisma } from "@/lib/prisma";
export async function POST() { const [bots, settings, channels] = await Promise.all([prisma.bot.findMany(), prisma.appSetting.findMany(), prisma.notificationChannel.findMany({ select: { id: true, name: true, type: true, enabled: true, createdAt: true, updatedAt: true } })]); return new NextResponse(JSON.stringify({ version: 1, exportedAt: new Date(), bots, settings, channels }, null, 2), { headers: { "content-type": "application/json", "content-disposition": "attachment; filename=bots-export.json" } }); }
