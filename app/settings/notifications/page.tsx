import { prisma } from "@/lib/prisma"; import { NotificationSettings } from "@/components/NotificationSettings";
export const dynamic = "force-dynamic"; export default async function Notifications() { let channels: { id: string; name: string; type: string; enabled: boolean }[] = []; try { channels = await prisma.notificationChannel.findMany({ select: { id: true, name: true, type: true, enabled: true } }); } catch {} return <><section className="pageHead compact"><div><p className="eyebrow">Delivery</p><h1>Notifications</h1><p>Connect the services that should receive your alerts.</p></div></section><NotificationSettings initial={channels} /></>; }
