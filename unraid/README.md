# Manual Unraid template testing

Copy `bots.xml` to `/boot/config/plugins/dockerMan/templates-user/my-bots.xml`, replace the placeholder repository URLs, then open **Docker → Add Container** and choose Bots from the template list. Keep `/data` mapped to `/mnt/user/appdata/bots` and port `3847` available.
